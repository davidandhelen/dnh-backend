type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  rsvpStatus: Boolean! @default(value: false)
  allowedPlusOnes: Number
  guestType: String!
  phone: String! @unique
  note: String
}