type User {
  id: ID! @id
  firstName: String!
  lastName: String!
  rsvpStatus: Boolean! @default(value: false)
  allowedPlusOnes: Int @default(value: 0)
  guestType: String!
  phone: String! @unique
  note: String
}